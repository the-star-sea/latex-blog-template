\documentclass{article}

% 版式与字体编码（若用 XeLaTeX/LuaLaTeX，可去掉 inputenc/fontenc，改用 fontspec）
\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath} 
% 图形与表格增强
\usepackage{graphicx}    % \includegraphics, \scalebox
\usepackage{booktabs}    % \toprule \midrule \cmidrule \bottomrule
\usepackage{multirow}    % \multirow
\usepackage{array}       % p{..}, m{..}, \newcolumntype, \arraybackslash
\usepackage{makecell}    % \makecell, 单元格内换行（可选）
\usepackage{siunitx}     % 数字对齐（可选，建议）
\usepackage{longtable}   % 可分页大表
\usepackage{stfloats}    % 双栏文档中 table* 浮动更灵活（可选）
\usepackage{float}       % H 浮动参数（可选）

% 标题样式
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{subcaption}  % 若有子图/子表再启用；否则可去掉

% 最后加载超链接
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=blue,
  citecolor=black
}

\begin{document}








\end{document}